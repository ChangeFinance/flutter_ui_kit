import 'package:flutter/material.dart';
import 'package:flutter_ui_kit/widgets/progress_timeline.dart';

class Timelines extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return const ProgressTimeline([]);
  }
}